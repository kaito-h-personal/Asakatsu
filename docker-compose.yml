version: '3'
services:
  web:
   image: nginx:alpine
   ports:
     - 8080:80
   volumes:
     - ./nginx_d/nginx.conf:/etc/nginx/conf.d/default.conf

  php:
   image: php:fpm-alpine
   volumes:
     - ./www_d/html:/var/www/html
